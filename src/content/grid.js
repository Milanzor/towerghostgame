// ---------------------------------------------------------------------------
// The playfield grid. Everything else (paths, towers, enemies) is measured in
// these tiles. Tweak COLS/ROWS to resize every room at once.
// ---------------------------------------------------------------------------
export const TILE = 64
export const COLS = 15
export const ROWS = 8
export const FIELD_W = COLS * TILE // 960
export const FIELD_H = ROWS * TILE // 512
